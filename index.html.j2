<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Schedule for {{ person.name }}</title>
    <link rel="stylesheet" href="style.css" media="screen">
    <link rel="stylesheet" href="print.css" media="print">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Lobster">
  </head>
  <body>
    <header>
      <div class="wrapper">
        <span class="schedule">ECE Visit Days Schedule</span> <h1 class="name">{{ person.name }}</h1>
      </div>
    </header>

    <div class="wrapper main">

  <table>
    <thead><tr><th></th><th>Time (EST)</th><th>Location</th><th>Name</th></tr></thead>
    <tbody>

%% for day in schedule:
  {% set ns = namespace(start_day=true) %}

  {% set day_loop = loop %}
%%  for timeslot in day.timeslots:
  {% set ts_loop = loop %}
%%    for event in (timeslot.meetings + timeslot.events):

## If this is the first event in the timeslot, then print the time
%%      if ns.start_day:
%%        set ns.start_day = false
          <tr class="day-start"><th>{{ day.date }}</th>
%%      else:
          <tr><th></th>
%%      endif

        <th>{{ timeslot.time }}</th>

%%          if event.is_meeting:
            <td><a href="{{ event.host.custom.zoom_link }}">Zoom &#x1F517;</a></td>
            <td>
              <b class="meeting">Meeting:</b>
%%              if person.role == "host":
%%                for visitor in event.visitors:
                    <a href="{{ visitor.custom.slate_link }}">{{ visitor.name }}</a>
%%                endfor
%%              else:
                    {{ event.host.name }}
## If the meeting is combined, mention that but don't include the name of the other user
                  {% if event.visitors | length > 1 %}(combined meeting){% endif %}
%%               endif
              </td>
%%           else:
              <td><a href="{{ event.location }}">Link &#x1F517;</a></td>
              <td>{{ event.name }}</td>
%%           endif
          </tr>
%%       endfor
%%     endfor
%% endfor
    </tbody></table>

</div>

    <footer>
        <div class="visigoth">Visigoth</div>
        <div>Visit Day Scheduler</div>
        <div><a href="https://www.gauravmanek.com/">Gaurav Manek</a></div>
    </footer>

  </body>
</html>
